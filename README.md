# KhoaiLaptop Supply

A self-contained ecommerce storefront for laptops, PC components, and desk gear.

## Structure

- `index.html` and `styles.css`: storefront experience
- `database/`: product catalog data
- `utils/`: cart state helper
- `admin/`: reserved administration area

Open `index.html` directly in a browser. No build step is required.
